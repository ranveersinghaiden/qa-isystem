# Local QA context tooling

This directory contains deterministic, repository-local context selection and
validation helpers. Workflows run tests only with read-only permissions.

Excluded integrations: remote issue lookup, hosted history or artifact
collection, and repository modification. Fixtures are synthetic and local.
